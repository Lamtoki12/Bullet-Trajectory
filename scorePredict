#include <stdio.h>
//#include<algorithm>

int main(){
	int x,y,z,m,n,a,b,dr,total = 100;
	printf("Masukan total win  jumlah main MU: ");
	scanf("%d %d", &m, &n);fflush(stdin);
	printf("Masukan total win  jumlah main Liverpool: ");
	scanf("%d %d", &a, &b);fflush(stdin);
	

	int mu = (m/n) * 100;
	int liv = (a/b) * 100;
	dr = 100 - mu - liv;

	/*	intinya jumlah menang dibagi sama jumlah main dikali seratus buat persentasenya 
		klo nyari persentase drawwnya persentase 100 - Persen menang MU - persen menagn liv
	*/
	/*if(m > a){
		x = m * 100;
	}else if(a > m){
		y = a * 100;
	}else if(m == a){
		z = dr * 100;
	}*/

	printf("MU win %d", &mu);
	printf("Liv win %d", &lif);
	printf("Draw win %d", &dr);
	getchar();
	return 0;
}
